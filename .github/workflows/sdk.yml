on:
  push:
    branches:
      - main

jobs:
  accept-licenses:
    runs-on: ubuntu-latest
    steps:
    - name: Accept all SDK licenses
      shell: bash
      run: printf 'y\ny\ny\ny\ny\n' | $ANDROID_HOME/tools/bin/sdkmanager --licenses
